package method;

public class MethodDocs {
	/*
	 * ---- Method -----
	 * 
	 * # used for code re-usability
	 * # lines or block of code which is used to perform some specific task in the program
	 *   is known as method.
	 *   
	 *   #syntax:
	 *   
	 *   		return_type method_name(arg1, arg2, ......, argn){
	 *   			//statements
	 *   		}
	 *   
	 *   #return type:
	 *   	
	 *   		a> void     : no value(result) returned from method
	 *   		b> non-void : value(result) returned from method
	 *   
	 */
	
	void print() {
			
	}
		
	void area(int l, int b) {
	}
	
	public static void main(String[] args) {
		
	}
}
